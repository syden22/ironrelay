# IronRelay by Syden — очередь фоновых задач и вебхуков для Django

**IronRelay** — это лёгкая и мощная система очередей и вебхуков для Django.
Работает без Celery, Redis и брокеров сообщений. Подходит для любых сайтов, CRM, API-интеграций и SaaS.

Автор: **Denys Sykhov (Syden)**  
© 2025

---

## 🚀 Возможности

### 🔄 Очередь фоновых задач (Task Queue)
- выполнение Python-функций в фоне  
- статусы задач: pending → running → failed / success  
- автоматические повторные попытки (retries)  
- логирование ошибок  
- очередь хранится в базе (SQLite/PostgreSQL/MySQL)  
- удобная админка + дашборд

### 📡 Исходящие вебхуки
- отправка POST запросов  
- история и статус доставки  
- количество попыток  
- автоматические повторы  
- логирование ошибок и ответов сервера

### 📥 Входящие вебхуки
Единый endpoint:

```
POST /ironrelay/incoming/<source>/
```

Сохраняет:
- источник  
- событие  
- payload  
- время получения  

И отправляет задачу в очередь на обработку.

### 📊 Дашборд IronRelay
Адрес:

```
/ironrelay/
```

Показывает:
- задачи  
- входящие вебхуки  
- исходящие вебхуки  

---

## ⚙️ Установка

```bash
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```

Запуск воркера:

```bash
python manage.py ironrelay_worker
```

---

## 📡 Пример входящего вебхука

```bash
curl -X POST http://127.0.0.1:8000/ironrelay/incoming/test/ \
     -H "Content-Type: application/json" \
     -d '{"event": "ping", "value": 123}'
```

---

## 🧩 Пример задачи

```python
from core.tasks import task

@task
def send_email(user_id):
    print("Send email to:", user_id)
```

Запуск:

```python
send_email.defer(10)
```

---

## 📑 Лицензия

MIT License  
Автор: **Denys Sykhov (Syden)**

---

## 📬 Поддержка

Email: **syxov22222222@gmail.com**

